import { BytetounitPipe } from './bytetounit.pipe';

describe('BytetounitPipe', () => {
  it('create an instance', () => {
    const pipe = new BytetounitPipe();
    expect(pipe).toBeTruthy();
  });
});
