import { Reference2Pipe } from './reference2.pipe';

describe('Reference2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Reference2Pipe();
    expect(pipe).toBeTruthy();
  });
});
