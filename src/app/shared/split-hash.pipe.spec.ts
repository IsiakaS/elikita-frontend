import { SplitHashPipe } from './split-hash.pipe';

describe('SplitHashPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitHashPipe();
    expect(pipe).toBeTruthy();
  });
});
