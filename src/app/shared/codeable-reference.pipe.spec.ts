import { CodeableReferencePipe } from './codeable-reference.pipe';

describe('CodeableReferencePipe', () => {
  it('create an instance', () => {
    const pipe = new CodeableReferencePipe();
    expect(pipe).toBeTruthy();
  });
});
