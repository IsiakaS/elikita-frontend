import { CodeableRef2Pipe } from './codeable-ref2.pipe';

describe('CodeableRef2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CodeableRef2Pipe();
    expect(pipe).toBeTruthy();
  });
});
