import { References2Pipe } from './references2.pipe';

describe('References2Pipe', () => {
  it('create an instance', () => {
    const pipe = new References2Pipe();
    expect(pipe).toBeTruthy();
  });
});
