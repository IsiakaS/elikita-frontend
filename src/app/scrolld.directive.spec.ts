import { ScrolldDirective } from './scrolld.directive';

describe('ScrolldDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolldDirective();
    expect(directive).toBeTruthy();
  });
});
